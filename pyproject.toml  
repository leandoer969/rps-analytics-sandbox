[tool.ruff]
target-version = "py311"
line-length = 100
exclude = ["dbt/target", "metabase-data", "volumes", ".venv"]

[tool.ruff.lint]
select = ["E","F","I","B","UP","SIM","W","C90"]
ignore = [
  "E203",  # handled by formatter
  "E501",  # use formatter for line length
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
skip-magic-trailing-comma = false
